# Audit archive

Curated final reports from every subagent run on this project (writers, auditors,
revisors, builders), extracted from session transcripts so the findings survive the
session. Raw transcripts (.raw.jsonl) are kept locally and gitignored (this repo is
public). Chronology and interpretation: ../PAPER_BUILD_LOG.md.
